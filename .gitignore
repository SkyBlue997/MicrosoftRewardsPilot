sessions/
dist/
node_modules/
notes
.DS_Store
# Temporary and log files
*.log
*.tmp
.env
# Local per-user config holding real credentials/secrets (keep only the .example templates in git)
config/accounts.json
config/accounts.dev.json
config/config.json
config/config.dev.json